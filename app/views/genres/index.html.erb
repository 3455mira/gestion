<p id="notice"><%= notice %></p>

<h1>ジャンル</h1>

<table>
  <thead>
    <tr>
      <th>ジャンルカラー</th>
      <th>画像</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @genres.each do |genre| %>
      <tr>
        <td><%= genre.color_g %></td>
        <td><%= image_tag show_image_genre_path(genre), :width => 50 %></td>
        <td><%= link_to '詳細', genre , target:"_self" %></td>
        <td><%= link_to '編集', edit_genre_path(genre) , target:"_self" %></td>
        <td><%= link_to '削除', genre, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'ジャンルを追加', new_genre_path , target:"_self" %>
