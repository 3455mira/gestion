<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ユーザー名 %>
    <%= form.text_field :name_user, id: :user_name_user %>
  </div>

  <div class="field">
    <%= form.label :メールアドレス %>
    <%= form.text_field :mail_user, id: :user_mail_user %>
  </div>

  <div class="field">
    <%= form.label :パスワード %>
    <%= form.password_field :pass_user, id: :user_pass_user %>
  </div>

  <div class="field">
    <%= form.label :アイコン %>
    <%= form.file_field :icon_user, id: :user_icon_user %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
